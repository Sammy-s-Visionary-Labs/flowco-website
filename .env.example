# Analytics remains dormant until account ownership and the privacy/consent
# approach are approved. Configure GA4 inside the production GTM container.
NEXT_PUBLIC_ANALYTICS_ENABLED=false
NEXT_PUBLIC_GTM_ID=

# Server-only Resend credential. The verified sender and testing recipient are
# static application configuration; never expose this key with NEXT_PUBLIC_.
RESEND_API_KEY=
